<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edgetechs.salonx">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/loopxblack"
        android:label="@string/app_name"
        android:roundIcon="@drawable/loopxblack"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".common.loginactivity.verifyLogin"></activity>
        <activity android:name=".common.loginactivity.user.userOtp" />
        <activity android:name=".common.loginactivity.user.userSignup" />
        <activity android:name=".common.loginactivity.user.userLogin" />
        <activity android:name=".usermain.userActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".merchantmain.MainActivity" />
        <activity android:name=".common.loginactivity.merchant.otp" />
        <activity android:name=".common.loginactivity.AskingPage" />
        <activity android:name=".common.sliderfragments.ScreenSliderAdapter" />
        <activity android:name=".common.loginactivity.merchant.confirmation2" />
        <activity android:name=".common.loginactivity.merchant.merchantSignUp2" />
        <activity android:name=".common.loginactivity.merchant.merchantSignUp1" />
        <activity
            android:name=".common.loginactivity.merchant.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".common.loginactivity.MainActivity" />
        <activity android:name=".common.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>